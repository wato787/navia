{
  "name": "bun-mise-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "apps/backend"
  ],
  "scripts": {
    "dev": "mise run dev",
    "build": "mise run build",
    "format": "bunx biome format --write",
    "lint": "bunx biome lint",
    "test": "bun run --filter @bun-mise/frontend test"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.2",
    "lefthook": "^2.0.2"
  }
}
