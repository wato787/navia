{
  "name": "bun-mise-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "apps/backend"
  ],
  "scripts": {
    "install": "mise run install",
    "dev": "mise run dev",
    "build": "mise run build"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.2"
  }
}
