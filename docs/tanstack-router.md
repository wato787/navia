# TanStack Router ?????

????????: [TanStack Router for React](https://tanstack.com/router/latest/docs/framework/react/quick-start)

## ??????

??????????????? TanStack Router ? Devtools ???????

```bash
npm install --workspace apps/frontend @tanstack/react-router @tanstack/router-devtools
```

## ????

`src/router.tsx` ??????????????????`App.tsx` ?? `RouterProvider` ?????????????????????

```tsx
// src/router.tsx
import {
  Link,
  Outlet,
  RootRoute,
  Route,
  createRouter
} from "@tanstack/react-router";
import { TanStackRouterDevtools } from "@tanstack/router-devtools";

const rootRoute = new RootRoute({ component: RootLayout });
const indexRoute = new Route({
  getParentRoute: () => rootRoute,
  path: "/",
  component: HomePage
});

const routeTree = rootRoute.addChildren([indexRoute]);
export const router = createRouter({ routeTree });

declare module "@tanstack/react-router" {
  interface Register {
    router: typeof router;
  }
}

function RootLayout() {
  return (
    <div>
      <header>
        <Link to="/">Home</Link>
      </header>
      <Outlet />
      <TanStackRouterDevtools position="bottom-right" />
    </div>
  );
}
```

```tsx
// src/App.tsx
import { RouterProvider } from "@tanstack/react-router";
import { router } from "./router";

export default function App() {
  return <RouterProvider router={router} />;
}
```

## ??????

1. ?????????????????: `src/pages/AboutPage.tsx`??
2. `Route` ?????`getParentRoute` ????? (`rootRoute`) ???????

```tsx
const aboutRoute = new Route({
  getParentRoute: () => rootRoute,
  path: "about",
  component: AboutPage
});

const routeTree = rootRoute.addChildren([indexRoute, aboutRoute]);
```

## ???????

- ?????? `Link` ??????????????`to` ?????????`activeProps` ????????????????????
- ??????????? `Outlet` ???????

```tsx
<Link to="/about" activeProps={{ style: { textDecoration: "underline" } }}>
  About
</Link>
```

## Devtools

`@tanstack/router-devtools` ? `TanStackRouterDevtools` ????????????????????????????????????????????????????? `import.meta.env.PROD` ???????

```tsx
{!import.meta.env.PROD ? <TanStackRouterDevtools position="bottom-right" /> : null}
```

## ???

`declare module "@tanstack/react-router"` ????? `Register` ????????? `router` ???????`Link` ? `useRouter` ??????????? API ????????

## ?????

- [Quick Start](https://tanstack.com/router/latest/docs/framework/react/quick-start)
- [API Reference](https://tanstack.com/router/latest/docs/api/router)
